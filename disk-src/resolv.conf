# Seeded into the disk image (PLAN §3, §6.5).
# MagicDNS resolver (Tailscale) first; public resolvers as fallback, reached through
# the selected exit node. ⚠VERIFY in Phase 5 whether CheerpX's lwIP intercepts DNS at
# 100.100.100.100 or passes it through.
nameserver 100.100.100.100
nameserver 1.1.1.1
nameserver 9.9.9.9
