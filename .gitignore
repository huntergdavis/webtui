node_modules/
dist/
.DS_Store
*.log
.env
.env.*

# Disk images: large generated artifacts (scripts/build-disk.sh). Not source.
*.ext2
public/disk/blocks/

# Self-hosted CheerpX engine: large binaries fetched by scripts/fetch-engine.sh.
# Commit ONLY the integrity manifest (the pinned hashes), never the binaries.
public/vendor/*
!public/vendor/engine.manifest.json

.vite/
